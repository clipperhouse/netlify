# Redirect default Netlify subdomain to primary domain
https://clipperhouse.netlify.com/* https://clipperhouse.com/:splat 302!

# These extra rules are required because HTTPS is not forced
http://clipperhouse.netlify.com/* https://clipperhouse.com/:splat 302!

/blog/* /:splat 302
